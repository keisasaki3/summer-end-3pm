# 午後三時、夏の果。 β 0.47

「みんなで歩けるだけ」の最小オンライン散歩空間。

## 技術

- Phaser 3
- TypeScript
- Vite
- Node.js
- ws (WebSocket)
- DBなし

## 開発モード

```powershell
npm install
npm run dev
```

PCブラウザ:

```text
http://localhost:5173
```

開発時だけ以下の2ポートを使います。

```text
5173 = Vite
8080 = WebSocket
```

---

# WAN公開モード

v0.04では本番時にゲーム画面とWebSocketを同じNode.jsサーバーから配信します。

## 1. ビルド

```powershell
npm run build
```

`dist` フォルダが生成されます。

## 2. 本番サーバー起動

```powershell
npm start
```

ブラウザで確認:

```text
http://localhost:8080
```

この状態では、

```text
8080
├─ ゲーム画面
└─ WebSocket
```

の1ポート構成です。

---

# Cloudflare Quick Tunnel でWAN公開

Cloudflare の `cloudflared` をインストールした後、

```powershell
cloudflared tunnel --url http://localhost:8080
```

を実行します。

すると、

```text
https://xxxxxxxx.trycloudflare.com
```

のようなURLが表示されます。

そのURLを友達へ送れば、別Wi-Fiや4G/5Gから同じオンライン空間へ入れます。

HTTPSで開いた場合、WebSocketも自動的に `wss://` で同一URLへ接続します。

## Cloudflared インストール例（Windows / winget）

```powershell
winget install --id Cloudflare.cloudflared
```

インストール後、一度PowerShellを開き直してください。

---

## v0.04変更点

- 本番時のゲーム画面とWebSocketを1ポート化
- 公開URLとWebSocket URLを同一オリジン化
- HTTPS公開時は自動的に `wss://` を使用
- `PORT` 環境変数にも対応
- Cloudflare Tunnel 1本だけでWAN公開可能

## 注意

Quick Tunnelはテスト用途向けです。
常時公開する段階になったら、Node.js/WebSocketをRailway等へデプロイする構成へ移行できます。


## v0.05
- 移動速度を 138 → 168 に調整
- 仮実装していた環境音・足音をすべて停止
- 画面下に簡易チャット入力欄を追加
- Enter送信でキャラクター頭上に吹き出し表示
- チャット内容をオンライン同期
- 吹き出しは約5秒で消える
- 最大80文字


## v0.06
- 名前とチャット吹き出しが左右反転しないよう修正
- 起動時に簡易ログイン画面を追加
- 名前を入力可能
- キャラ色を6色から選択可能
- 身長を「低め / ふつう / 高め」の3段階から選択可能
- 名前・色・身長はWebSocketで他プレイヤーにも同期
- 未ログイン状態の接続は世界に出現させない


## v0.07
- ログイン画面入力中は WASD / 矢印キー移動を無効化
- ゲーム中に Enter を押すとチャット入力欄へフォーカス
- チャットのプレースホルダーを `Say something...` に変更
- 野良猫モブを1匹追加
- 野良犬モブを1匹追加
- 建物・川周辺に簡易当たり判定を追加
- 当たり判定は現在のプロシージャルマップに合わせた矩形ベース


## β 0.08
- ブラウザタイトルを `午後三時、夏の果。 β 0.47` に変更
- ゲーム内にも `β 0.08` 表示
- 当たり判定を移動処理へ実際に接続
- 建物6棟 / 駅・線路 / 川 / 橋の柵 / 木に当たり判定
- X/Y軸を別々に解決し、壁沿いに自然に滑る方式
- 猫は長めに休んでから短く素早く移動する個性
- 犬は広めの範囲をゆっくり気ままに移動する個性
- 猫・犬グラフィックを目・鼻・耳・腹・尻尾まで描き直し
- Enterでチャットへフォーカス
- チャット中はPhaserのキーボード入力を無効化し、WASDも普通に文字入力可能
- Enterで送信してフィールド操作へ戻る
- Escで送信せずフィールド操作へ戻る
- チャット文言を `Speak into the evening...` に変更


## β 0.09
- β 0.08の起動停止バグを修正
- 猫の尻尾描画で存在しないPhaserメソッドを呼んでいた問題を修正
- ログインUIを猫犬生成より先に作るよう起動順を安全化
- 既存のβ 0.08機能は維持


## β 0.47
- チャット入力中のキーボード競合を再修正
- DOM input フォーカス中は Phaser Keyboard Plugin を完全停止
- keydown / keyup / keypress を入力欄で止め、A/W/S/D/矢印キーも文字入力へ
- Enterでチャットへ、Enter送信後はフィールド操作へ
- Escで送信せずフィールド操作へ

## β 0.47
提供された夕凪町画像を一枚絵フィールドとして採用。画像に沿った当たり判定・カメラ追従・猫犬再配置。

## β 0.47
Late Summer at the Pier を夕凪町BGMとしてループ再生。ログイン開始操作後に再生し、ブラウザ自動再生制限に対応。音量0.32。

## β 0.47
- ログイン/チャット入力中は Phaser KeyboardPlugin を完全停止
- WASD・矢印・Space の Phaser capture を解除
- DOM入力中は移動処理自体も停止
- BGMデフォルト音量 12%
- OPTIONSウィンドウ追加。BGMスライダー/MUTE、localStorage保存
- 論理Viewportを960x540固定、Phaser FIT + CENTER_BOTHでレスポンシブ拡大
- 大画面でも見えるゲーム世界の範囲を増やさず、没入感を維持

## β 0.47 — クイズ・夕凪町 / 夏円 / 固定画面
- 15秒後に初回、以後約60秒ごとに町内教養クイズをサーバーから全員へ同時配信
- 4択・回答受付20秒・1問1回答
- 正解で100夏円。所持金はログイン（接続）ごとに0へリセット
- 判定と所持金加算はサーバー側で実施
- 画面左上に夏円HUD
- 960×540固定表示。ブラウザ拡大に追従してゲームキャンバスを引き伸ばさない
- WASD/矢印はPhaserのブラウザキーCaptureを使わず、DOM入力と分離

## β 0.47
- 町内教養クイズの通常出題間隔を60秒から180秒（3分）へ変更
- 問題プールを12問から100問へ拡張
- 直前と同じ問題が連続出題されないように調整

## β 0.47 — 夕凪町背景更新
- ユーザー提供の `edd02152-e296-449d-a09e-f071c5cde488.png` を加工せず、そのまま `public/yunagicho.png` として採用
- 1536×864の新背景に合わせて当たり判定を全面再作成
- 中央交差点・奥へ続く路地・海沿い道路・橋・左側歩道/神社階段を歩行領域として設計
- 建物、植栽、新聞社、バス停本体、護岸、水面、前景住宅を侵入不可に再設定
- クイズ100問・3分間隔・夏円・BGM・OPTIONS・固定960×540画面を維持

## β 0.47 — PC画面仕様確定
- 論理Viewportを1280×720（16:9）に変更
- PCでの最大表示サイズも1280×720
- 1280×720より大きいブラウザではアップスケールしない
- 小さいブラウザでのみ16:9を維持して縮小
- World（夕凪町1536×864）とViewportを分離。カメラ追従で町を歩く構造を維持

## β 0.47 — 木漏れ日神社
- ユーザー提供画像をそのまま `public/komorebi-jinja.png` として新マップ化
- 新マップ名「木漏れ日神社」
- 夕凪町左上の神社階段を通行可能にし、上端から木漏れ日神社へマップ移動
- 木漏れ日神社の南参道から夕凪町へ戻れる
- 木漏れ日神社専用の当たり判定を新規作成
- 町内クイズUIは夕凪町にいる時だけ表示
- 1280×720固定Viewport / 1536×864 Worldを維持

## β 0.47 — マップ別データ/同期
- プレイヤーに `map` を持たせ、サーバーがマップ単位で snapshot / join / move / leave / chat を配信
- 別マップのプレイヤーは表示されない。移動時は旧マップからleave、新マップへjoin
- マップ定義 `mapData` を追加（名称・背景・犬/猫・クイズ有無）
- 夕凪町：犬のみ / クイズあり
- 木漏れ日神社：猫のみ / クイズなし
- 夕凪町左上の神社周辺の当たり判定を大幅撤去し、鳥居～階段～上端を広く通行可能化

## β 0.47 — チャットログ
- 左下に小さな半透明暗色チャットログを追加
- 自分の送信発言と、そのマップで実際に受信した発言を蓄積
- 最新6行表示、内部30件保持
- マップ移動後も自分が受け取った履歴は保持
- 通常時は薄く、チャット入力フォーカス中のみ少し濃く表示

## β 0.47 — コンビニ
- 新マップ「コンビニ」を追加
- 提供画像をそのまま public/convenience-store.png として使用
- 夕凪町の真ん中下の道路と、コンビニ駐車場中央下を相互接続
- コンビニ専用の当たり判定を追加
- map-aware multiplayer に convenience を追加

## β 0.47 — build fix
- main.ts に誤って混入したリテラル `\n` を実改行へ修正
- β0.22 のコンビニ実装内容は維持

## β 0.47 — chat / portal fix
- チャットログの他プレイヤー名が [object Object] になる不具合を修正
- 原因: playerName に文字列ではなく Phaser Text オブジェクトを保存していた
- 夕凪町→コンビニの出口を中央下から少し右の道路（x=850〜1010）へ移動
- コンビニ→夕凪町の復帰地点も道路側 x=930 に変更

## β 0.47 — 夕凪町クイズ更新
- マップ表示名「コンビニ　深夜」を「コンビニ」に修正
- クイズ・夕凪町の問題100問を全面差し替え
- 難度を上げ、歴史・哲学・経済・政治・法・自然科学・数学統計・情報・心理社会・宗教・芸術文学などから出題
- 出題間隔・4択・正解100夏円など既存ルールは維持

## β 0.47 — コンビニ入口微調整
- 夕凪町→コンビニのワープ判定を右へキャラ約1体分（32px）移動
- 判定幅をそこからキャラ約3体分（96px）に変更
- 判定範囲: x=882〜978

## β 0.47 — 座標デバッグ
- 夕凪町→コンビニ移動時のスポーンをコンビニ右端側へ変更（X:1420 / Y:700）
- 画面右上に現在マップ名・X/Y座標を表示
- OPTIONSに「座標を表示」ON/OFFを追加
- 座標表示はデフォルトON
- 設定は localStorage に保存

## β 0.47 — コンビニ往復位置修正
- 夕凪町→コンビニのワープ判定中心を X:1076 / Y:848 に変更
- 判定幅は中心から左右キャラ約2人分（各64px）、X:1012〜1140
- コンビニ→夕凪町の帰還先も同じ入口中心 X:1076 に統一
- コンビニ側スポーンは右端側 X:1440 に調整

## β 0.47 — コンビニ往復バグ修正
- コンビニ→夕凪町の出口を中央下から右端側へ移動
- コンビニ側出口: X 1376〜1504 / 下端（中心 X 1440）
- 夕凪町→コンビニの到着地点も同じ右端側 X 1440 に統一
- 夕凪町への帰還座標を X 1076 / Y 800 にして、ワープ直後に境界・再ワープ判定へ食い込んで動けなくなる問題を修正
- 夕凪町側入口判定 X 1012〜1140（中心 X 1076）は維持

## β 0.47 — ワンクリックアップデーター
- `update.bat` を正式導入
- 今後の差分配布ファイル名を `update.zip` に固定
- `update.zip` をプロジェクト直下へ置いて `update.bat` をダブルクリックするだけ
- 自動解凍 → 上書き → `npm run build` → 8080番ポートの旧サーバー停止 → `npm start`
- 通常の差分更新では `npm install` 不要

## β 0.47 — 夕凪町コンビニ接続道路の衝突修正
- 夕凪町 X927〜1208、Y760〜下端を明示的な完全歩行可能領域に変更
- 同範囲へかかっていた下右側の大型ポリゴン当たり判定を撤去
- コンビニから夕凪町へ戻った直後に動けなくなる問題を解消

## β 0.47 — 正式キャラクタースプライト
- 添付画像を加工せず `public/sprites/teddy-bear.png` として正式実装
- 従来の図形キャラクター表示を廃止
- ログイン画面に「種族」を追加し、初期種族「テディぐま」を選択可能
- 4方向（正面・左・右・背面）と歩行アニメーションを実装
- 名前・身長3段階・チャット・マップ・オンライン同期など既存システムを維持
- race をオンラインプレイヤーデータへ追加

## β 0.47 — テディぐま透明化バグ修正
- crop済み画像へ setDisplaySize を適用していた表示処理を廃止
- スプライトシート実寸基準の crop + scale 表示へ変更
- 1050px高を整数境界で4方向へ分割し、Phaserのcrop矩形を安定化

## β 0.47 — テディぐま足元・スプライト分割修正
- 実ファイル寸法 1437×1095px を再検証
- 旧実装の誤った1400×1050前提を廃止
- 元シートはそのまま保持し、実寸から7列×4行を正確に個別フレーム化
- Phaserのcrop表示を廃止し、各フレームを独立Textureとして表示
- キャラクターの原点を足元に固定し、影と足の位置を一致
- 歩行時の上下ボブを廃止して「空中を飛ぶ」見え方を解消
- 歩行3コマ＋立ち4コマを4方向で実装

## β 0.47 — テディぐまアニメーション／足元／素材クリーンアップ
- 他プレイヤーも毎フレーム歩行・立ちアニメーションするよう修正
- 自分・他人とも上下移動ベクトルをアニメーションへ渡し、正面/背面へ正しく向くよう修正
- 元スプライトシートの各キャラを連結成分から再抽出し、隣接フレーム断片や孤立したゴミを除去
- 明らかな赤・緑の生成アーティファクトをランタイム用フレームから除去
- 全28フレームを同一260pxキャンバスの同一足元ベースラインへ正規化
- 旧キャラ由来の図形は使用していない。疑似影も撤去
- スプライトの足元をプレイヤー座標に完全固定し、浮遊して見える問題を修正

## β 0.47 — 「いにしえロボット」族
- 添付画像を `public/sprites/ancient-robot.png` として原本そのまま保存
- 実寸から7列×4方向、28フレームのランタイム用スプライトを生成
- ログイン画面の種族選択を「テディぐま」「いにしえロボット」の2種へ拡張
- いにしえロボットも4方向、歩行3コマ＋立ち4コマでアニメーション
- 自分・他プレイヤーとも選択した種族をオンライン同期
- 既存のテディぐま、マップ、チャット、クイズ、ワープ等は維持

## β 0.47 — 立ちアニメーション頻度調整
- 停止中の立ちアニメーション常時ループを廃止
- 通常時は立ちフレームで完全静止
- 停止後4.5〜11秒のランダム間隔で、約1.2秒の立ちアニメーションを1回だけ再生
- 再生後は再び静止し、次回のタイミングをランダムに再設定
- 自分・他プレイヤー、テディぐま・いにしえロボット共通仕様
- 移動中の歩行アニメーションは従来どおり継続

## β 0.47 — 犬・猫モブ廃止
- 夕凪町の野良犬モブを完全撤去
- 木漏れ日神社の野良猫モブを完全撤去
- 犬猫の生成、移動AI、マップ別表示フラグ、関連状態をコードから削除
- プレイヤー種族・マップ・チャット・クイズ等には変更なし

## β 0.47 — いにしえロボット描画修正
- ロボットのランタイムフレーム生成方式を再設計
- 連結成分によるキャラ抽出を廃止（謎の矩形・断片混入の原因）
- 元スプライトを7列×4行のセル境界で直接切り出し
- セル境界に接続した小さな不要断片のみ除去
- 28フレームを統一透明キャンバス・同一足元ラインへ再配置
- 元の `ancient-robot.png` は変更なし

## β 0.47 — テディぐま差し替え
- テディぐまの正式原本を新しい添付画像へ差し替え
- 新レイアウト（上段=歩行3コマ×4方向 / 下段=立ち4コマ×4方向）に合わせてランタイム用28フレームを再生成
- 既存コードの 0〜2=歩行 / 3〜6=立ち アニメ規約は維持
- 元ファイル `public/sprites/teddy-bear.png` は添付画像そのまま保存

## β 0.47 — 仕様書反映
- プロダクト表記を `午後三時、夏の果。` へ統一
- package名 / ブラウザタイトル / updater表示も `午後三時、夏の果。` へ変更
- 旧 `vw-*` localStorage は読み取り互換を残し、新規保存は `nantoka-*` へ移行
- 種族固有の隠し移動速度パラメータを実装
  - テディぐま: 168
  - いにしえロボット: 176（テディぐまよりわずかに速い）
- 種族ごとのテクスチャPrefixと移動速度を `raceData` に集約
- キャラクタースプライト暫定仕様 v0.1 をプロジェクトへ追加

## β 0.47 — テディぐま歩行アニメーション修正
- 新テディぐま原本の実レイアウトを再検証
- 768×2048画像を192×256の固定セル（4列×8行）として処理
- 上半分の歩行ブロックは各行の先頭3セルのみ使用
- 下半分の立ちブロックは4セルすべて使用
- 壊れていた歩行3コマ目を含む全28フレームを再生成

## β 0.47 — キャラクタースプライト仕様 v1.0
- スプライトTODOを全件実装し、暫定v0.1から正式v1.0へ更新
- 正式シートを768×2048、セルを192×256へ固定
- ランタイムを200×270、足元基準Y=268へ固定
- 標準表示高84px、種族別visualScale / sizeClassを導入
- 正式命名規則を導入
- テディぐま・いにしえロボットを正式規格へ正規化
- `npm run validate:sprites` を追加
- `npm run build` 前にスプライト自動検品を実施
- SPEC.md の完了済みTODOを削除

## β 0.47 — テディぐま明滅バグ修正
- 原因: 新テディぐま原本は192×256の等間隔セルではなく、キャラクターが可変間隔で配置されていた
- 固定セル切り出しを廃止
- 原本の透過領域から28体のキャラクター本体を連結成分として個別抽出
- 各フレームへ隣接キャラクターの足・頭などが混入しないよう、本体成分だけを保持
- 歩行12コマ / 立ち16コマを正しい方向・順序で再構成
- 全フレームを200×270、足元Y=268へ再正規化
- 正式シート `sheets/teddy.png` も同じ抽出結果から再生成

## β 0.47 — 新種族「うさぎjk」
- ユーザー提供のキャラチップ画像を原本のまま `public/sprites/source/rabbit-jk-original.png` に保存
- 画像生成は行わず、提供画像から28体を直接抽出して実装
- 4方向 ×（歩行3 + 立ち4）= 28フレーム
- ランタイム規格 200×270 / 足元Y=268 に正規化
- 正式シート `public/sprites/sheets/rabbit-jk.png` を生成
- ログイン画面の種族選択へ「うさぎjk」を追加
- WebSocket同期で `rabbit-jk` を正式サポート
- 隠し移動速度はテディぐまと同じ168
- スプライト自動検品対象へ追加

## β 0.47 — うさぎjk描画修正 / 教養クイズ一時停止
- `animateWalker()` に残っていた「ロボット以外はテディぐま」の旧分岐を削除
- 歩行/立ちアニメーション時も `raceData.texturePrefix` を参照するよう統一
- これにより `rabbit-jk` 選択時にテディぐまへ差し戻される不具合を修正
- 教養クイズを一時停止
- サーバーの出題タイマー停止
- クイズ回答処理停止
- クライアントのクイズUI生成停止
- クイズ関連パケット表示処理停止

## β 0.47 — Presence安定化 / BGM撤去 / マップ音響基盤
- 長時間放置後に他プレイヤーが消える問題への対策を実装
- クライアント12秒heartbeat + authoritative presence再同期
- サーバー25秒WebSocket ping/pong keepalive
- 接続断時の自動再接続（1/2/4/8秒バックオフ）
- 再接続時に現在マップ・座標・種族を復元
- `Late Summer at the Pier.mp3` と従来BGM再生処理を削除
- OPTIONSからBGMスライダー/MUTEを削除
- 全マップを一旦無音化
- マップごとに `bgmKey` / `ambienceKeys` を設定できる音響基盤を追加
- updaterに `.nantoka-delete.txt` による不要ファイル削除機能を追加


## 正式ゲーム名
- 正式ゲーム名を **「午後三時、夏の果。」** に統一
- 末尾の句点「。」までを正式名称として扱う
- ブラウザタイトル、サーバー表示、アップデータ、仕様書・READMEの旧称を統一
- ログイン画面上部に「午後三時、夏の果。」を控えめに表示
